# frozen_string_literal: true

source 'https://rubygems.org'

gem 'bundler'
gem 'os' # reports what os someone is using
gem 'rake'
gem 'thor' # CLI gem

group :test do
  gem 'guard'
  gem 'guard-minitest'
end
